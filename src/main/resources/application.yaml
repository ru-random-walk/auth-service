auth:
  google:
    google-auth-url: https://www.googleapis.com/oauth2/v3

jwt:
  expire-time-in-seconds: 900
  refresh-token-expire-time-in-days: 7

spring:
  application:
    name: AuthService
  flyway:
    enabled: true

server:
  port: 8080